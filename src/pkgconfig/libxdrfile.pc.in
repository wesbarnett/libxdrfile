prefix=MYPREFIX
libdir=${prefix}/lib
includedir=${prefix}/include

Name: libxdrfile
Description: Reads/writes compressed trajectory files
URL: https://github.com/wesbarnett/libxdrfile
Version: MYVERSION

Requires:
Libs: -L${libdir} -lxdrfile
Cflags: -I${includedir}
Cflags.private: -fPIC -shared
